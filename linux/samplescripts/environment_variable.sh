#!/bin/bash

echo "The environment variable PATH is $PATH"
echo "The environment variable HOME is $HOME"
echo "The environment variable USER is $USER"
echo "The environment variable SHELL tells us which SHELL we are using $SHELL"
cat /etc/shells # tells us all the available shells"
